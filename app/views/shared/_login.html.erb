<%= form_for :session, url: '/login', html: {id: "login_form"} do |f| %>
  <% if flash[:error] %>
  <div id = "login_error" class = "error-message">
    <%= flash[:error] %>
  </div>
  <% end %>
  <%= f.label :username %>
  <%= f.text_field :username %>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <%= f.submit "Log in", :class => "button" %>
<% end %>
<%= link_to 'Register', register_path, class: 'centered', "data-role" => "button", "data-ajax" => "false" %>
<%= form_for :session, url: '/login', html: {id: "login_form_example"} do |f| %>
  <%= f.hidden_field :username, value: Student.first.username if Student.first %>
  <%= f.hidden_field :password, value: "password1234" %>
  <%= f.submit "Demo", class: "button" %>
<% end %>
